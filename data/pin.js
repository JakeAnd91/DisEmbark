class Pin {
  constructor(tag, type, color, position, userName) {
    this.tag = tag;
    this.type = type;
    this.color = color;
    this.position = position;
    this.userName = userName;
  }
}

export { Pin };
